=image_tag @pic.name.url(:jumbo),id:'picture_cropbox' if @pic = @user.pictures.first

= form_for @user do |f|
 =f.fields_for :picture do |ff|
  - %w[x y w h].each do |attribute|
   = ff.hidden_field "crop_#{attribute}"
  =f.submit "Save chages"
