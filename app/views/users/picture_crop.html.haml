=image_tag @pic.name.url(:large),id:'cropbox' if @pic = @user.pictures.first

= form_for @user.pictures do |f|
  - %w[x y w h].each do |attribute|
    = f.text_field "crop_#{attribute}"
  =f.submit "Save chages"
