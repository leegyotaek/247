<%= form_for current_user.friendships.find_by(friend_id: @user.id) , method: :delete do |f| %>
  <div><%= hidden_field_tag :friend_id, @user.id %></div>
  <div class="form_field">
  <%= f.submit "신청취소", class: "btn btn-warning" %>
  </div>
<% end %>