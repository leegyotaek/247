<%= form_for current_user.friendships.find_by(friend_id: @user) , method: :delete do |f| %>
  <div><%= hidden_field_tag :friend_id, @user.id %></div>
  <%= f.submit "친구해제", class: "btn btn-default btn-warning" %>
<% end %>