<h3>오늘의 매칭</h3>

<ul class="matchers_list">
<% @today_matchers.each do |matcher| %>
<%=render 'pending_matcher' , matcher: matcher %>
<% end %>
</ul>
<h3> 나와 1:1 매칭을 원하는 친구</h3>
<ul class="req_matchers_list">
<% if @req_matchers.any? %>
<% @req_matchers.each do |req_matcher| %>
<%=render 'req_matcher' ,  matcher: req_matcher%>
<% end %>
</ul>
<% else %>
	no requests

<% end %>
<h3> 성사된 외국인 친구</h3>


<% if current_user.matchers.any? %>

<% current_user.matched_matchers.each do |matcher| %>
<ul class="matched_matchers_list">
<%= render 'matcher' , matcher: matcher  %>
</ul>
<% end %>


<% end %>